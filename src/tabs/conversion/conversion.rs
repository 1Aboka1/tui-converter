pub enum Conversion {
    Binary(String), 
    Octadecimal(String),
    Decimal(String),
    Hexadecimal(String),
}

impl Conversion {
    pub fn convert(numbers: Conversion) {
                     
    }
}
